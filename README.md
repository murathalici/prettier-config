# `@mhalici3/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @mhalici3/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@mhalici3/prettier-config"
}
```
